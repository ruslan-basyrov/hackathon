# Conversion Coach — runtime dependencies (Insurance AI / UNIQA track)
#
# This is the CPU-side runtime: the simulation harness, the GBM detector, the
# OpenAI-compatible LLM client, and the NiceGUI demo. It is everything you need
# to reproduce the headline results (baseline + per-persona uplift + GBM ablation).
#
# Mirrors pyproject.toml. If you have `uv`, prefer `uv sync` (uses uv.lock for
# exact pins); this file is the pip-equivalent for a clean `python -m venv`.
#
# The LoRA fine-tune environment (torch / transformers / peft / trl) is GPU-heavy
# and Leonardo-only — it is intentionally NOT listed here. Install it separately
# with `pip install -e .[lora]` on the cluster (see README "Persona-bot fine-tune").

# --- core harness: state machine, signals, runner, config ---
pyyaml>=6.0

# --- GBM detection (Phase 3) + training/eval metrics ---
numpy>=2.2.6
xgboost>=3.2.0
scikit-learn>=1.3

# --- LLM client: coach wording (Phase 4) + persona bots (Phase 5) ---
# OpenAI-compatible; talks to vLLM / Ollama / a remote endpoint via
# INFERENCE_BASE_URL + MODEL_NAME. No cloud provider is a hard dependency.
openai>=2.38.0

# --- NiceGUI demo viewer (Phase 3.5): / debug + /journey on-stage demo ---
nicegui>=3.12.1

# --- experiment tracking (optional; train_gbm logs here if a key is present) ---
wandb>=0.16

# --- tests + headed demo driver ---
pytest>=8.0
playwright>=1.40            # `playwright install chromium` once, for the UI tests
pytest-playwright>=0.5
